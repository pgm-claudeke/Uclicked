{  
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "sass": "^1.74.1",
    "vite": "^5.2.8",
    "vite-plugin-restart": "^0.4.0"
  }
}
